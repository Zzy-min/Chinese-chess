services:
  - type: web
    name: xiangqi-web
    runtime: docker
    plan: free
    autoDeploy: true
    dockerfilePath: ./Dockerfile
    envVars:
      - key: BIND_HOST
        value: 0.0.0.0
      - key: JAVA_TOOL_OPTIONS
        value: -Xms128m -Xmx512m
