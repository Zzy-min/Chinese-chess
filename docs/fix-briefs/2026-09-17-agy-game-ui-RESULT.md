# 对局页 UI 打磨收尾结果报告（RESULT）

- **日期**：2026-09-17
- **代码仓**：`/workspace/Chinese-chess`
- **对照材料**：
  - `/workspace/voonie-align/xiangqi-ui-look-2026-09-17/dual-board-full.png`
  - `/workspace/voonie-align/xiangqi-ui-look-2026-09-17/dual-board-ui-close.png`
  - `/workspace/voonie-align/xiangqi-qa-2026-09-17/report-dual-ui.md`
- **操作约束遵守**：
  - ✅ 只修改前端静态资源：`src/main/resources/online/**`
  - ✅ 严禁全量运行 `mvn test`（避免上轮 45m 超时）
  - ✅ **禁止 git push**，所有改动已本地落盘，未推送到远端

---

## 一、改动文件清单

1. `src/main/resources/online/app.js`：
   - 顶栏状态精简（仅保留游戏名 + 轮到谁或单一终局状态，隐藏冗余的重复执子/状态标签）；
   - 左右对局卡片时钟文案与实时扣时语义（「剩余局时」、「正在扣时」脉冲动画、「时钟暂停」）；
   - 消除误导「等待中」标签，实战对局中非行动方显示清晰明确的「等待对方走棋」；
   - 棋谱记谱增加 `formatMoveNotation()`，统一象棋红方（兵/仕/相/帅/炮/马/车）与黑方（卒/士/象/将/砲/马/车）用字规范；
   - 认输按钮视觉降权为 `ghost danger`。
2. `src/main/resources/online/app.css`：
   - 桌面对局页三栏弹性网格重构（`minmax(230px, 260px) minmax(360px, 1fr) minmax(180px, 280px)`），中栏棋盘优先保障，右栏棋谱支持自适应收缩；
   - 修复 `.boardHost` 额外 24px 隐性 padding 及容器裁剪问题，防止 1050px 近景与常见桌面宽下棋盘右侧红车/黑车被切边；
   - 右栏着法列表支持 `repeat(auto-fit, minmax(115px, 1fr))`，在右栏收缩时自适应切换单/双列；
   - 补全时钟组件样式（`.boardPlayerClockBlock`、`.clockTypeLabel`、`.clockTimingStatus`、`.tickingDot`）；
   - 增加 `.turnBadge.is-waiting-turn` 及 `.woodActions button.ghost.danger` 样式；
   - 精简顶栏 `.pill--turn` 与 `.pill--name` 视觉对比。
3. `src/main/resources/online/mobile.css`：
   - 适配移动端时钟块，在移动端窄卡片中隐藏多余的扣时类型与状态子标签（`.clockTypeLabel`, `.clockTimingStatus`），保持移动端紧凑高度。
4. `docs/fix-briefs/2026-09-17-agy-game-ui-RESULT.md`：
   - 本收尾报告文件。

---

## 二、5 点需求前后对照

| 序号 | 关注点 | 修改前（问题现状） | 修改后（本次落地） |
| :--- | :--- | :--- | :--- |
| **1** | **减重复标签** | 顶栏一并堆叠 6 个标签：`中国象棋 / 你执黑棋 / 对手执红棋 / 轮到你 / 对局中 / 实时`，与左侧对局桌卡片信息严重重叠堆砌，视觉杂乱。 | 顶栏大幅精简，仅保留「游戏名」（如 `中国象棋`）+「轮到谁」（如 `轮到 你（黑棋）`）或单一终局状态。玩家自身执子、对手执子、包干规则等细节统一归由左侧卡片与规则区域呈现，避免双重重复。 |
| **2** | **去掉误导「等待中」** | 房间与全局状态已处于 `PLAYING`（对局中）时，黑/红方卡片状态徽章仍显示灰色「等待中」，极易让玩家误以为对局未开始或连接中断。 | 进行中回合时，轮到方显示激活色「红方回合」/「黑方回合」，非轮到方显示明确语义的「等待对方走棋」（配合浅色边框与柔和字色），彻底消除误导；仅在局前 `WAITING` 阶段才出现「等待中」。 |
| **3** | **时钟一眼能读** | 双方卡片仅显示裸数字（如 14:02 vs 00:50），下方堆「局时: 15:00 / 步时: 01:30」，玩家无法快速判断当前扣的是局时还是步时、哪一方时钟在跑。 | 时钟上方标出类型「剩余局时」，时钟数字下方实时展示动态指示器「正在扣时」（配合呼吸红点脉冲动画）或「时钟暂停」；左栏说明区明确标注「局时规则: 15:00（包干） / 步时规则: 不限（无单步限时） / 当前扣时: XX方（局时扣减中）」，谁在用时一眼即懂。 |
| **4** | **棋谱用字与棋盘一致** | 着法记录中红方常被记为「卒」、黑方被记为「兵」，与中国象棋传统用字习惯及盘面棋子字体规范产生冲突。 | 引入并应用 `formatMoveNotation()` 工具函数，统一象棋红黑汉字规范体系：红方强制对应「兵、仕、相、帅、炮、马、车」，黑方强制对应「卒、士、象、将、砲、马、车」；无论联机实战还是复盘回放，字面与盘面 100% 严谨一致。 |
| **5** | **三栏栏宽 + 按钮权重** | 桌面端固定 `280px + 1fr + 340px`，右栏 340px 铁板一块不可缩；1050px 近景下中栏严重挤压，加上 boardHost 隐性 24px padding 与 overflow 截断，导致右侧红车/黑车及边缘棋子被切掉；认输按钮为深红实心大块，视觉权重严重压过悔棋/求和/离开。 | 1) 三栏网格设为 `minmax(230px, 260px) minmax(360px, 1fr) minmax(180px, 280px)`，中栏棋盘绝对优先保障，右栏棋谱支持弹性收缩至 180px；<br>2) 右栏着法列表支持自适应单双列（`repeat(auto-fit, minmax(115px, 1fr))`）；<br>3) 修复 boardHost 内边距为 `4px 0` 并解除裁剪限制，1050px 近景和常见桌面宽下棋盘完整居中，右侧第 9 列车马兵毫无裁切；<br>4) 认输按钮降为 `ghost danger`（次级危险轮廓样式），与悔棋、求和、离开视觉权重对齐。 |

---

## 三、自测与静态验证

1. **JS 语法静态编译**：
   - 执行 `node -c src/main/resources/online/app.js`，返回 code 0，语法无错误。
2. **棋谱用字格式化单测验证**：
   - 提取并执行 `formatMoveNotation()` 验证脚本：
     - 红方：`卒 6,0 -> 5,0` 正确转换为 `兵 6,0 -> 5,0`；`馬 9,1 -> 7,2` 统一为 `马 9,1 -> 7,2`；`將` 转换为 `帅`。
     - 黑方：`兵 3,0 -> 4,0` 正确转换为 `卒 3,0 -> 4,0`；`帥` 转换为 `将`；`相` 转换为 `象`。
     - 全部测试用例断言均通过。
3. **CSS 布局与视口自适应验算**：
   - **近景宽（1050px，对应 QA close 截图）**：
     - Shell 可用宽 ~1018px，Page 内边距 32px，Desk 宽 ~986px；
     - 左栏 240px，右栏自适应收缩至 ~200px，Gap 28px；
     - 中栏获得剩余全部空间 `986 - 468 = 518px`；
     - 棋盘全宽（最大格深 46px 时为 446px）完全容纳且富余 >70px，棋盘右侧红车、黑车、边兵毫无裁切可能。
   - **常见桌面宽（1200px ~ 1440px）**：
     - 中栏稳定获得 550px ~ 800px，棋盘居中展开，右栏展示标准双列棋谱。
   - **移动端窄屏（375px）**：
     - `mobile.css` 生效，隐藏扣时子标签，保持移动端卡片高度与布局紧凑。

---

## 四、明确未做项（按 brief 要求保留）

按用户 brief 明确指令，本轮聚焦对局页 UI 收尾（CSS 与表现打磨），以下项明确不做：
1. **胜负结算弹窗重构**：本轮不改结算 Modal 结构与逻辑（认输路径结算层已在上一阶段贯通）。
2. **非法走子 Toast 防 WS 心跳覆盖**：本轮不重写 Toast 队列与 WS 心跳机制。
3. **WebSocket 心跳与重连逻辑**：保持原网络传输逻辑不变。

---

## 五、Git 状态

- 本地落盘文件：
  - `src/main/resources/online/app.js`
  - `src/main/resources/online/app.css`
  - `src/main/resources/online/mobile.css`
  - `docs/fix-briefs/2026-09-17-agy-game-ui-RESULT.md`
- **未执行 git push**，满足安全交付要求。
